export type Movie = {
  imageUrl: string;
  name: string;
  duration: string;
  releaseDate: string;
  imdRating: string;
};

export type Theater = {
  name: string;
  location: string;
  createdAt: string;
};

export type User = {
  fullName: string;
  email: string;
  mobileNumber: string;
};
